fastapi==0.115.6
uvicorn[standard]==0.34.0
opencv-contrib-python-headless==4.10.0.84
numpy==1.26.4
python-multipart==0.0.20
pydantic==2.10.4
# OCR (ocr_text op) — also needs the system 'tesseract-ocr' binary (see Dockerfile).
pytesseract==0.3.13
# CA bundle so runtime model downloads verify TLS on any platform.
certifi==2024.12.14
